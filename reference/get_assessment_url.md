# Get assessment URL for historical MEAP data

Returns the URL for historical MEAP assessment files. Note: These URLs
exist but michigan.gov CDN may block programmatic access.

## Usage

``` r
get_assessment_url(end_year)
```

## Arguments

- end_year:

  School year end

## Value

URL string or NULL if not available
